#spring.application.name=todo-rest-api
spring:
  application:
    name: todo-rest-api

  datasource:
    url: jdbc:mysql://localhost:3306/irusri-todo
    username: amila
    password: amila
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

api:
  endpoint:
    base-url: http://localhost:8080







#  profiles:
#    active: dev
#  servlet:
#    multipart:
#      max-file-size: 50MB
#springdoc:
#  default-produces-media-type: application/json